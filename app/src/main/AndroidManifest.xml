<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.fileencryption">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FileEncryption">
        <activity android:name=".DeleteFile"></activity>
        <activity android:name=".CaesCipher" />
        <activity android:name=".CvtNumber" />
        <activity android:name=".CvtTxtToBin" />
        <activity android:name=".Binary" />
        <activity android:name=".NewAct" />
        <activity android:name=".DisplayImage" />
        <activity android:name=".ChooseFile" />
        <activity android:name=".DecryptActivity" />
        <activity android:name=".EncryptActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>